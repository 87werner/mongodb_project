<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    {{>Nav}}
    <h1>Congratulations you have created your account</h1>
  {{#each thisPost}}
    <form action="/userPostUpdate/{{this._id}}" method="POST">
        <input type="text" name="blogTitle" placeholder="title{{this.title}}">
        <input type="text" name="blogBody" placeholder="blog body{{this.body}}">
        <button type="submit"> Edit blog</button>
    </form>
  
    {{/each}}
    
</body>
</html>